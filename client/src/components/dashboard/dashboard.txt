        {profile.loading && profile.profile === null ?(
            console.log(`Loading ${profile.loading} current profile: ${profile.profile}`)
        ):(
            <Fragment>
                { profile.profile !== null ?(
                    <Fragment>
                        {console.log(profile.profile)}
                       <DashboardLayout  profile={profile.profile} auth={auth}/>  
                    </Fragment>
                ) : (
                    <Fragment> 
                        {console.log(profile.profile)}
                        <Redirect from='/dashboard' to='/create-profile' />
                    </Fragment>
                )};
            </Fragment>
        )};
    </Fragment>